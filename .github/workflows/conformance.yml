# =============================================================================
# Absolute Valheim Server - conformance with the Absolute engineering standard
# =============================================================================
# Checks .absolute/policy.yml against the standard, and both against this
# repository. See https://github.com/abspwgm/absolute-standard
# =============================================================================

name: Conformance

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  conformance:
    name: Conformance
    uses: abspwgm/absolute-standard/.github/workflows/conformance.yml@54f604f2696ee6a82146b4658ee9eede647406d4
    with:
      standard-ref: 54f604f2696ee6a82146b4658ee9eede647406d4
